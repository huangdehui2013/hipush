upstream hipush_admin {
	{{#admins}}
	server {{ address }}:{{ port }} max_fails=2 fail_timeout=30s weight=10;
	{{/admins}}
}

upstream hipush_web {
	{{#webs}}
	server {{ address }}:{{ port }} max_fails=2 fail_timeout=30s weight=10;
	{{/webs}}
}